# RN Inspector Monorepo

This repository hosts the CLI, proxy, UI, and React Native example app for RN Inspector.

## Packages

- `packages/cli` – Node CLI entrypoint
- `packages/proxy` – WS/HTTP proxy and logger
- `packages/ui` – React (Electron/web) frontend